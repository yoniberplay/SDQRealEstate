@using SDQRealEstate.Core.Application.ViewModels.User;
@model UserViewModel
@{
    ViewData["Title"] = "Eliminar Agente";
}

<div class="row">

    <div class="col-6 offset-3" >
        <div class="card">
            <div class="card-header bg-primary text-light">
                <h4>¿Está seguro que desea eliminar este agente @Model.Username y todas sus propiedades?</h4>
            </div>
            <div class="card-body">

                <form method="post" asp-controller="Admin" asp-action="Eliminar">
                
                    <input type="hidden" asp-for="Id" />

                    <div class="text-center">
                    <button type="submit" class="btn btn-danger">Eliminar</button>
                    <a asp-controller="Admin" asp-action="agentes" class="btn btn-secondary ">Cancelar</a>
                    </div>
                </form>

            </div>
        </div>
    </div>

</div>

