@using SDQRealEstate.Core.Application.ViewModels.User;
@model UserViewModel
@{
    ViewData["Title"] = "Eliminar Agente";
}

<div 
    class='bg- d-flex flex-row justify-content-center align-items-center' 
    style="width: 100%; min-height: 83.9vh; height: auto"
>

    <!-- Section -->
    <section class="w-50 mx-auto d-flex flex-column justify-content-center align-items-center gap-2 p-2">
        <h2 class="">¡Espera!</h2>
        <small class='w-75 text-muted text-center'>¿Está seguro que desea eliminar al agente @Model.Username y todas sus propiedades?</small>

        <form method="post" asp-controller="Admin" asp-action="EliminarAgente" class="w-100 mt-3">
                
            <input type="hidden" asp-for="Id" />

            <div class="text-center w-100">
                <button type="submit" class="btn btn-light w-25">Eliminar</button>
                <a asp-controller="Admin" asp-action="agentes" class="btn btn-success w-25">Cancelar</a>
            </div>
        </form>
    </section>

    <!-- Image of Error -->
    <section class="w-75 h-100  p-0 rounded overflow-hidden mx-4">
        <img src='/Images/Predeterminated/error.png' alt='main login image' class='w-100' />
    </section>

</div>

